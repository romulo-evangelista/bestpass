import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  // Password Styles
  password: {
    height: 40,
    width: '90%',
    borderWidth: 2,
    borderColor: 'white',
    borderRadius: 10,
    color: 'white',
  },
});
